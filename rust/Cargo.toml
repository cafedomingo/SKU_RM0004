[package]
name = "display"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "display"
path = "src/main.rs"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["ioctl", "fs", "net"] }

[profile.release]
opt-level = "s"
strip = true
